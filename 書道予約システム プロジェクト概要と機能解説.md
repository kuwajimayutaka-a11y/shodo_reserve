# 書道予約システム プロジェクト概要と機能解説

## 1. プロジェクトの基本構造

| 要素 | 説明 |
| :--- | :--- |
| **フレームワーク** | Django 5.2 (Python 3.11) |
| **データベース** | SQLite（開発） / PostgreSQL（本番: Render） |
| **認証** | カスタムUserモデル（メールアドレス＋パスワード） |
| **主要アプリ** | `booking` |
| **ユーザー種別** | **保護者**（一般ユーザー）と **管理者**（`is_staff=True`） |
| **デプロイ** | Docker + Render |

## 2. データモデル

| モデル | フィールド | 説明 |
| :--- | :--- | :--- |
| `CustomUser` | email, name, password, is_active, is_staff | ログインID=メールアドレス、氏名必須 |
| `Family` | user(OneToOne), phone_number | 保護者アカウントと生徒グループの紐付け |
| `Student` | family(FK), name | 実際に授業を受ける生徒（兄弟対応） |
| `LessonSlot` | title, start_time, end_time, capacity, reservation_start_time | 授業枠 |
| `Reservation` | lesson_slot(FK), student(FK), reserved_at | 予約（二重予約防止制約あり） |
| `Waitlist` | lesson_slot(FK), student(FK), waitlisted_at | 補欠登録（登録順ソート） |

## 3. 認証・登録フロー

### 保護者の新規登録
1. `/signup/` で氏名・メールアドレス・パスワードを入力
2. アカウントを `is_active=False` で作成し、確認メールを送信
3. メール内のリンク（`/accounts/verify/<token>/`）をクリック → `is_active=True`
4. `/accounts/login/` へリダイレクト（「認証完了」メッセージ表示）

### ログイン後のリダイレクト
| ユーザー種別 | 条件 | リダイレクト先 |
| :--- | :--- | :--- |
| 管理者 | `is_staff=True` | `/admin-dashboard/` |
| 保護者 | 生徒登録あり | `/calendar/` |
| 保護者 | 生徒登録なし | `/students/`（追加を促す） |

### 管理者アカウント作成
- `create_admin.py` をデプロイ時に自動実行
- 環境変数 `DJANGO_ADMIN_EMAIL` / `DJANGO_ADMIN_USER`（氏名） / `DJANGO_ADMIN_PASSWORD` を使用

## 4. 機能一覧

### 4.1. 保護者機能

| 機能 | URL | 概要 |
| :--- | :--- | :--- |
| 新規登録 | `/signup/` | 氏名・メアド・パスワードで登録、メール認証あり |
| ログイン/ログアウト | `/accounts/login/` | メールアドレス＋パスワード認証 |
| 予約カレンダー | `/calendar/` | 日別表示。予約・補欠登録が可能 |
| 生徒情報 | `/students/` | 自分のアカウントに紐づく生徒の管理 |
| 生徒追加 | `/students/add/` | 生徒を登録する |
| 生徒編集/削除 | `/students/<id>/edit/` | 登録済みの生徒情報を編集・削除 |
| 予約キャンセル | `/reservations/<id>/cancel/` | 予約のキャンセル |

### 4.2. 管理者機能

| 機能 | URL | 概要 |
| :--- | :--- | :--- |
| ダッシュボード | `/admin-dashboard/` | 統計情報と管理メニューへのリンク |
| 授業枠一括作成 | `/admin-dashboard/create-lesson/` | 期間・曜日・複数時間帯を指定して一括作成 |
| 授業枠個別作成 | `/admin-dashboard/create-lesson-single/` | 単発授業枠の作成 |
| 授業枠一覧 | `/admin-dashboard/lessons/` | 全授業枠の一覧・編集・削除 |
| 予約一覧 | `/admin-dashboard/reservations/` | 全予約の一覧表示・キャンセル |
| 生徒管理 | `/admin-dashboard/students/` | 全保護者と紐づく生徒の管理 |
| 管理者カレンダー | `/admin-dashboard/calendar/` | 全生徒対象の予約作成（Select2対応） |

## 5. セキュリティ

- 管理者機能: `@user_passes_test(is_staff)` で保護
- 保護者ページ: 管理者がアクセスした場合は `/admin-dashboard/` へリダイレクト
- 保護者は自分の Family に紐づく Student のみ操作可能
- CSRF保護: 全フォームに `{% csrf_token %}`
- メール認証: 登録後は `is_active=False`、メールリンクで有効化

## 6. 未実装・今後の課題

| 課題 | 優先度 | 概要 |
| :--- | :--- | :--- |
| 補欠→本予約の自動繰り上げ | 高 | キャンセル発生時に補欠リストの先頭を自動昇格 |
| 管理者によるユーザー作成 | 中 | 管理者がアカウントを作成して保護者に通知 |
| 生徒追加申請フロー | 中 | 保護者の生徒追加を申請制にして管理者が承認 |
| パスワードリセット | 中 | メールによるパスワード再設定 |
| メール通知 | 低 | 予約完了・キャンセル時のメール通知 |

## 7. 環境変数（Render）

| 変数名 | 用途 |
| :--- | :--- |
| `DATABASE_URL` | PostgreSQL接続文字列（自動設定） |
| `SECRET_KEY` | Djangoシークレットキー（自動生成） |
| `DJANGO_ADMIN_EMAIL` | 管理者メールアドレス |
| `DJANGO_ADMIN_USER` | 管理者氏名 |
| `DJANGO_ADMIN_PASSWORD` | 管理者パスワード |
| `EMAIL_HOST_USER` | SMTP送信元メールアドレス（Gmailなど） |
| `EMAIL_HOST_PASSWORD` | SMTPパスワード（Gmailアプリパスワード） |

---
**最終更新日**: 2026年4月27日
